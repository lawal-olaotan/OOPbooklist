<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">

    <% if (typeof elements !== "undefined" && elements.length) {
        elements.map(el=>{
           if(el.endsWith('.css')){
               if(!el.match('//')  ){ %>

       <link rel="stylesheet" href="<%= path+el %>">
       <% } else {%>
           <link rel="stylesheet" href="<%= el %>">
           <% }}})  } %>
    
 
    <title><%- title %></title>
</head>
    <body class="body" data-bs-spy="scroll" data-bs-target="#secnavbar" data-bs-offset="20" tabindex="0">
       
    <%-body%>

        <% if (typeof elements !== undefined && elements.length) {
            elements.map(el=>{
                if(el.endsWith('.js') ){
                    if(!el.match('//')  ){ 
        %>
                <script src="<%= path+el %>"></script>
                <% }
        else { %>
                    <script src="<%= el %>"></script>
                    <% }  }}) } %>
    
    </body>
    
</html>